{
  "name": "panpantechnology.com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node scripts/validate-site.mjs",
    "check": "npm run build && npm test"
  }
}
